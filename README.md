# Axis Football — Minimal Prototype

This is a minimal Next.js (App Router) + Tailwind prototype that reads data from local JSON files.

## Quick start

1. Install dependencies
```bash
npm install
```

2. Run dev server
```bash
npm run dev
```

Open http://localhost:3000

The project uses local JSON files in `/data`.
